
<%= simple_form_for [ @product, @booking ] do |f| %>
  <%= f.input :date %>
  <%= f.submit "Submit booking", class: "btn btn-search ml-1" %>
  <%= link_to 'Back to product', product_path(@product), class: "btn btn-ghost" %>
<% end %>
